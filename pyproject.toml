[tool.poetry]
name = "todo-list"
version = "0.1.0"
description = ""
authors = ["Younes Kazemi M. <ykazemim04@gmail.com>"]
readme = "README.md"
packages = [{ include = "todo_list" }]

[tool.poetry.dependencies]
python = ">=3.12"
python-dotenv = "^1.1.1"
sqlalchemy = "^2.0.0"
psycopg2-binary = "^2.9.0"
alembic = "^1.13.0"
schedule = "^1.2.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
